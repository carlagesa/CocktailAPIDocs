export const metadata = {
  title: 'Cocktail Endpoints',
  description: 'Explore the various endpoints available for managing and searching cocktails.',
}

# Cocktails

Cocktail endpoints allow you to manage and search for cocktails. Here, you can retrieve, search, filter, and list cocktails based on different criteria.

## List all cocktails {{ tag: 'GET', label: '/v1/cocktails-only' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a list of all cocktails. 

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/cocktails-only">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/cocktails-only \
      -H "Authorization: Bearer {token}"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.cocktails.list()
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.cocktails.list()
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->cocktails->list();
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "cocktail_001",
          "name": "Mojito",
          "ingredients": ["Mint", "Lime", "Sugar", "Rum"],
          "instructions": "Muddle mint and lime, add sugar and rum, top with soda water.",
          "image_url": "https://example.com/images/mojito.jpg",
          "created_at": 692233200,
          "updated_at": 705103200
        }
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## Search cocktails by name {{ tag: 'GET', label: '/v1/search/cocktail' }}

<Row>
  <Col>

    This endpoint allows you to search for cocktails by name.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/search/cocktail">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/search/cocktail \
      -H "Authorization: Bearer {token}" \
      -d name="Mojito"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.cocktails.searchByName('Mojito')
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.cocktails.search_by_name(name="Mojito")
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->cocktails->search_by_name('Mojito');
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "cocktail_001",
          "name": "Mojito",
          "ingredients": ["Mint", "Lime", "Sugar", "Rum"],
          "instructions": "Muddle mint and lime, add sugar and rum, top with soda water.",
          "image_url": "https://example.com/images/mojito.jpg",
          "created_at": 692233200,
          "updated_at": 705103200
        }
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## Search cocktails by first letter {{ tag: 'GET', label: '/v1/search/letter' }}

<Row>
  <Col>

    This endpoint allows you to list cocktails that start with a specific letter.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/search/letter">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/search/letter \
      -H "Authorization: Bearer {token}" \
      -d letter="M"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.cocktails.searchByFirstLetter('M')
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.cocktails.search_by_first_letter(letter="M")
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->cocktails->search_by_first_letter('M');
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "cocktail_001",
          "name": "Mojito",
          "ingredients": ["Mint", "Lime", "Sugar", "Rum"],
          "instructions": "Muddle mint and lime, add sugar and rum, top with soda water.",
          "image_url": "https://example.com/images/mojito.jpg",
          "created_at": 692233200,
          "updated_at": 705103200
        }
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## Search ingredients by name {{ tag: 'GET', label: '/v1/search/ingredient' }}

<Row>
  <Col>

    This endpoint allows you to search for ingredients by name.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/search/ingredient">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/search/ingredient \
      -H "Authorization: Bearer {token}" \
      -d name="Mint"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.ingredients.searchByName('Mint')
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.ingredients.search_by_name(name="Mint")
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->ingredients->search_by_name('Mint');
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "ingredient_001",
          "name": "Mint",
          "description": "A refreshing herb.",
          "image_url": "https://example.com/images/mint.jpg"
        }
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## Filter cocktails by ingredient {{ tag: 'GET', label: '/v1/filter/ingredient' }}

<Row>
  <Col>

    This endpoint allows you to filter cocktails by ingredient.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/filter/ingredient">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/filter/ingredient \
      -H "Authorization: Bearer {token}" \
      -d ingredient="Mint"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.cocktails.filterByIngredient('Mint')
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.cocktails.filter_by_ingredient(ingredient="Mint")
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->cocktails->filter_by_ingredient('Mint');
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "cocktail_001",
          "name": "Mojito",
          "ingredients": ["Mint", "Lime", "Sugar", "Rum"],
          "instructions": "Muddle mint and lime, add sugar and rum, top with soda water.",
          "image_url": "https://example.com/images/mojito.jpg",
          "created_at": 692233200,
          "updated_at": 705103200
        }
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## Filter cocktails by alcoholic {{ tag: 'GET', label: '/v1/filter/alcoholic' }}

<Row>
  <Col>

    This endpoint allows you to filter cocktails based on whether they are alcoholic or non-alcoholic.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/filter/alcoholic">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/filter/alcoholic \
      -H "Authorization: Bearer {token}" \
      -d alcoholic=true
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.cocktails.filterByAlcoholic(true)
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.cocktails.filter_by_alcoholic(alcoholic=True)
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->cocktails->filter_by_alcoholic(true);
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "cocktail_001",
          "name": "Mojito",
          "ingredients": ["Mint", "Lime", "Sugar", "Rum"],
          "instructions": "Muddle mint and lime, add sugar and rum, top with soda water.",
          "image_url": "https://example.com/images/mojito.jpg",
          "created_at": 692233200,
          "updated_at": 705103200
        }
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## Filter cocktails by category {{ tag: 'GET', label: '/v1/filter/category' }}

<Row>
  <Col>

    This endpoint allows you to filter cocktails based on their category.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/filter/category">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/filter/category \
      -H "Authorization: Bearer {token}" \
      -d category="Cocktail"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.cocktails.filterByCategory('Cocktail')
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.cocktails.filter_by_category(category="Cocktail")
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->cocktails->filter_by_category('Cocktail');
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "cocktail_001",
          "name": "Mojito",
          "ingredients": ["Mint", "Lime", "Sugar", "Rum"],
          "instructions": "Muddle mint and lime, add sugar and rum, top with soda water.",
          "image_url": "https://example.com/images/mojito.jpg",
          "created_at": 692233200,
          "updated_at": 705103200
        }
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## Filter cocktails by glass {{ tag: 'GET', label: '/v1/filter/glass' }}

<Row>
  <Col>

    This endpoint allows you to filter cocktails based on the type of glass used.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/filter/glass">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/filter/glass \
      -H "Authorization: Bearer {token}" \
      -d glass="Highball"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.cocktails.filterByGlass('Highball')
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.cocktails.filter_by_glass(glass="Highball")
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->cocktails->filter_by_glass('Highball');
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "cocktail_001",
          "name": "Mojito",
          "ingredients": ["Mint", "Lime", "Sugar", "Rum"],
          "instructions": "Muddle mint and lime, add sugar and rum, top with soda water.",
          "image_url": "https://example.com/images/mojito.jpg",
          "created_at": 692233200,
          "updated_at": 705103200
        }
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## List categories {{ tag: 'GET', label: '/v1/list/categories' }}

<Row>
  <Col>

    This endpoint allows you to list all available categories for cocktails.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/list/categories">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/list/categories \
      -H "Authorization: Bearer {token}"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.categories.list()
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.categories.list()
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->categories->list();
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "category_001",
          "name": "Cocktail"
        },
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## List glasses {{ tag: 'GET', label: '/v1/list/glasses' }}

<Row>
  <Col>

    This endpoint allows you to list all available glasses for cocktails.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/list/glasses">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/list/glasses \
      -H "Authorization: Bearer {token}"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.glasses.list()
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.glasses.list()
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->glasses->list();
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "glass_001",
          "name": "Highball"
        },
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## List ingredients {{ tag: 'GET', label: '/v1/list/ingredients' }}

<Row>
  <Col>

    This endpoint allows you to list all available ingredients.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/list/ingredients">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/list/ingredients \
      -H "Authorization: Bearer {token}"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.ingredients.list()
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.ingredients.list()
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->ingredients->list();
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "ingredient_001",
          "name": "Mint"
        },
        // ...
      ]
    }
    ```

  </Col>
</Row>

---

## List alcoholic filters {{ tag: 'GET', label: '/v1/list/alcoholic' }}

<Row>
  <Col>

    This endpoint allows you to list all alcoholic filters.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/list/alcoholic">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/list/alcoholic \
      -H "Authorization: Bearer {token}"
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.alcoholic.list()
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.alcoholic.list()
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->alcoholic->list();
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "alcoholic_001",
          "name": "Alcoholic"
        },
        // ...
      ]
    }
    ```

  </Col>
</Row>
